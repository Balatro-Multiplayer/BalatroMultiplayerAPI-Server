# URL of the BalatroMultiplayerAPI-Server (server-side proxy target)
API_SERVER_URL=http://localhost:8788

# Public API base seen by the browser (proxied through Next.js)
NEXT_PUBLIC_API_BASE=/api/proxy
