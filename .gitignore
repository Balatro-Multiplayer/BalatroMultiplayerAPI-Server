node_modules
.env
dist
.vscode
releases
.vs
logs
data
!.env.example

# Logs
*.log

# Turbo cache
.turbo/

# Editor
.vs/
*.local*
!docker-compose.local.yml
# Personal Claude Code settings only (API keys, local permission overrides) -
# .claude/commands/, .claude/skills/, etc. are meant to be shared/committed,
# unlike the blanket ".claude" this replaces which blocked all of it.
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Clients
clients/*
